mutation GetApiAccessToken($clientId:String!,$secret:String!){
    getApiAccessToken(clientId:$clientId,secret:$secret){
        access_token
        expires_in
        refresh_token
        scope
        token_type
    }
}